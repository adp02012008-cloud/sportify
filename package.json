{
  "name": "soundwave-platform",
  "version": "1.0.0",
  "private": true,
  "description": "SoundWave — Production-Quality Music, Podcast, and Audiobook Streaming Platform",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd server && npm install --include=dev && npm run build",
    "build:all": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
